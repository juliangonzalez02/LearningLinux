# Basic Traversal
1. First I discovered where I was when I first opened the terminal, ```pwd```
2. Then I listed the files and directories that I could travel to. As well as list them in more detail and include hidden files, ```ls -la```
3. I changed directories to the documents folder and made changes to this repo, ```cd Documents/``` ```cd LearnLinux/```
4. Utilized ```mkdir``` to create a directory within the LearnLinux dir in order to better organize the basic administration commands
5. Leveraged ```touch traversal.txt``` to create a text file of traversal commands for this repo
6. Realized that I wanted the text file to be in Markdown, ```mv traversal.txt traversal.md```
7. Created a duplicate file in */basic-commands* by editing the .txt instead of .md so I then promptly deleted and renamed, ```rm traversal.md; mv traversal.txt traversal.md```